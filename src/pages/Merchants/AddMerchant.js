import React from 'react'
import withTimeout from '../../hoc/withTimeout'
import Layout from '../../components/layout/Layout'

const AddMerchant = () => {
  return (
    <Layout title="Add Merchant">AddMerchant</Layout>
  )
}

export default withTimeout(AddMerchant)