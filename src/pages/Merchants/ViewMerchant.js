import React from 'react'
import withTimeout from '../../hoc/withTimeout'
import Layout from '../../components/layout/Layout'

const ViewMerchant = () => {
  return (
    <Layout title="Merchants">ViewMerchant</Layout>
  )
}

export default withTimeout(ViewMerchant)