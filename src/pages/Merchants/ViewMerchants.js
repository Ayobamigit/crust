import React from 'react'
import withTimeout from '../../hoc/withTimeout'
import Layout from '../../components/layout/Layout'

const ViewMerchants = () => {
  return (
    <Layout title="Merchants">ViewMerchants</Layout>
  )
}

export default withTimeout(ViewMerchants)